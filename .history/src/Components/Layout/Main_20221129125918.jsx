import React from 'react';
import { Outlet } from 'react-router-dom';
import Navbar from '../Navbar';

const Main = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Outlet></Outlet>
        <Fo
        </div>
    );
};

export default Main;